import React from 'react';
import Content from '../content/content';
import Header from '../header/header';

const Main:React.FC = () => {
    return (
        <div>
            <Header/>
            <Content/>
        </div>
    );
}

export default Main;